{
  "name": "create-weld-app",
  "version": "0.10.0",
  "description": "Create a new WELD app — npm create weld-app",
  "type": "module",
  "bin": {
    "create-weld-app": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "prepublishOnly": "pnpm build"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
