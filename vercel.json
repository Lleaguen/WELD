{
  "buildCommand": "cd docs && npm install --ignore-scripts && npm install sharp --foreground-scripts && ./node_modules/.bin/astro build",
  "outputDirectory": "docs/dist",
  "installCommand": "echo skip"
}
