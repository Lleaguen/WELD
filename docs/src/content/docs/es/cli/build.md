---
title: weld build
description: Compilá tu app WELD para producción.
---

```bash
weld build
```

Ejecuta `vite build` para producción. La salida va a `dist/`.

Detecta automáticamente tu gestor de paquetes (npm, pnpm, bun).
